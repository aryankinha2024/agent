# Server Agent Configuration
# 
# AGENT_TOKEN: Secret bearer token for API authentication
# Generate a secure token with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AGENT_TOKEN=your-secure-random-token-here

# PORT: HTTP port the agent listens on (exposed via Cloudflare Tunnel)
PORT=4000

# NODE_ENV: Environment mode (production recommended)
NODE_ENV=production
